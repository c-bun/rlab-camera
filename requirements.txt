# Core web app (dev machine + Pi)
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
apscheduler>=3.10
pillow>=10.0
numpy>=1.24
tifffile>=2023.7.10  # ImageJ-readable TIFF export (embeds capture settings)
bleak>=0.21  # Bluetooth LE control of the LED illumination panels (BlueZ on the Pi)

# On the Raspberry Pi, picamera2 is installed via the system package manager,
# not pip:
#   sudo apt install -y python3-picamera2
# It is intentionally NOT listed here so this file installs cleanly on macOS.
